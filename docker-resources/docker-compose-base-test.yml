services:
  db:
    image: postgres:15-alpine
    ports:
      - "54355:5432"
    environment:
      POSTGRES_DB: rotterdam-oracle-ebs-test
      POSTGRES_USER: test-user
      POSTGRES_PASSWORD: password
